# node_next_technical_challenge

## Run

### With docker
```sh
docker compose -f docker-compose.yml up
```

### Without docker

#### Start the backend
```sh
cd backend/api && yarn start:dev 
```

#### Start the frontend
```sh
cd frontend && yarn start:dev 
```